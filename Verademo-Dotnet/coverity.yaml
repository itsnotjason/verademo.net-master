capture:
  build:
    clean-command: dotnet msbuild /t:Restore;Clean ${{ github.workspace }}/Verademo-dotnet.csproj
    build-command: dotnet msbuild /t:Restore;Build ${{ github.workspace }}/Verademo-dotnet.csproj
analyze:
  checkers:
    webapp-security:
      enabled: true
